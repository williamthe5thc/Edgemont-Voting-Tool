// api/config.js

// Define categories directly in the API to ensure consistency
export const API_CATEGORIES = [
    'Bread',
    'Appetizers',
    'Dessert',
    'Entrée & Soups'
];

// Add other API-specific constants here
export const API_CONFIG = {
    MAX_VOTES_PER_CATEGORY: 2,
    MIN_DISHES_PER_CATEGORY: 1,
    MAX_DISHES_PER_CATEGORY: 60
};